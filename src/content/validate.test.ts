import { describe, expect, it } from 'vitest'; import { validateQuestionBank, validateStoredQuestionBank } from './validate'; import type { StoredQuestionBank } from './schema';
describe('question bank validation',()=>{it('detects duplicate IDs and invalid answers',()=>{const q={id:'q',quizId:'z',stem:'x',choices:[{id:'A',text:'a'},{id:'A',text:'b'}],sourceAnswer:'Z',rationale:'R',metadata:{}};const results=validateQuestionBank([{id:'s',name:'S',accent:''}],[{id:'z',subjectId:'s',name:'Z',questionCount:2}],[q,q]);expect(results.filter(x=>x.level==='error').length).toBeGreaterThan(2)});it('rejects derived fields in stored schema version 3',()=>{const bank={schemaVersion:3,subjects:[{id:'s',name:'S',accent:'',description:''}],quizzes:[{id:'z',subjectId:'s',name:'Z',questionCount:1}],questions:[{id:'q',quizId:'z',subjectId:'s',questionNumber:1,answerSource:'provided_key',stem:'x',choices:[],rationale:'R',metadata:{}}]} as unknown as StoredQuestionBank;const results=validateStoredQuestionBank(bank);expect(results.filter(x=>x.level==='error')).toHaveLength(6)});});
